const LANG = 'fr';

let labels;

if(LANG === 'fr') {

	labels = {
		remove: 'Supprimer',
		download: 'Télécharger',
		latestMod: 'Dernière modification le'
	};

}

else if(LANG === 'en') {
	
	labels = {
		remove: 'Delete',
		download: 'Download',
		latestMod: 'Latest modification at'
	};

}